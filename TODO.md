# Compass（建築工程管理PM） - 課題・タスク管理

## 最終更新: 2026-02-15

## Critical（即対応）

- [ ] **価格体系変更（2026-02-15確定: 席単価→社単位ティア制）**
  - Small（〜5名）: ¥5,000/月
  - Standard（〜15名）: ¥15,000/月
  - Business（〜40名）: ¥35,000〜/月（Gen連携・AI機能搭載後）
  - Enterprise（100名〜）: 要相談
  - Stripe Product/Price作成（Small・Standardを先に）
  - チェックアウト画面のプラン選択UI実装
  - 既存席単価ユーザーのSmallプランへの移行
  - IT導入補助金の申請対象（3/30期限）
  - 導入コンサルティング・研修を役務として登録

## High（1-2週間以内）

- [ ] タスク可視性バグ調査・修正
  - タスクの表示・非表示が意図通りに動作しないケースの特定
  - 権限・フィルタ条件の確認
  - 再現手順の文書化と修正

## Medium（今月中）

- [ ] デバッグログ削除（UserManagement.tsx）
  - 開発時のconsole.log等のデバッグ出力を本番コードから除去
  - UserManagement.tsx を中心に確認

## Low（今四半期中）

（なし）

## 完了済み

### COMPASS × Google Chat × Googleドライブ 連携（案件作成の二度手間解消）

#### ゴール
COMPASSでプロジェクト（案件）を作成したら、同時に
- Googleドライブ：案件フォルダ作成
- Google Chat：案件スペース作成
を行い、**各URLをCOMPASSのプロジェクトに保存してワンクリックで飛べる**ようにする。

#### 基本方針
- 連携は **org（組織）単位**でON/OFF・ルール設定できるようにする（「使いたい組織だけ各々セット」）。
- **プロジェクトのオーナーorgが連携ONのときだけ** 自動作成する。
- 招待された側（参加者側）は原則「作らない」。オーナーが作ったChat/Driveのリンクに乗る。

#### MVP（まず作る）
1) **org設定（Google連携設定画面）**
- Drive
  - enabled
  - parentFolderId（共有ドライブ内のフォルダID等）
  - folderNameTemplate（例：`{projectType}_{projectName}`）
- Chat
  - enabled
  - spaceNameTemplate（例：`【COMPASS】{projectName}`）
  - （任意）初期メッセージテンプレ（COMPASSプロジェクトURL等）
- memberSyncMode（まずは `none` / `addOnly` のみ）

2) **プロジェクト作成時の自動作成**（オーナーorgがONの場合）
- Driveフォルダ作成 → `driveFolderUrl` をプロジェクトに保存
- Chatスペース作成 → `chatSpaceUrl` をプロジェクトに保存
- プロジェクト画面から「Driveを開く」「Chatを開く」導線を追加

3) **Chatの招待メンバー選択（PJ単位）**
- PJ設定に「Chatに招待するメンバー（メール）」のマルチセレクト（チェック）を追加
- まずは addOnly（追加のみ）で招待
- Googleメールが未設定のメンバーはスキップ＋警告表示

#### Phase2（運用しながら拡張）
- memberSyncModeを拡張：`addAndRemove`（削除同期）
- Drive共有権限の自動同期（PJメンバー→フォルダ共有）
- 招待された側のオプション（必要なら）：個人/自組織用の「ミラー作成」

### プロジェクト種別ごとのマイルストーン設定（実装済み: 2026-01-15〜2026-01-16 / d8ce01d, 4344d17）

プロジェクト作成時に種別を選択し、種別ごとに異なるマイルストーンを設定できるようにする。

**種別案:**

| 種別 | マイルストーン |
|------|---------------|
| 内装案件 | 現地調査→レイアウト確定→基本設計→設計施工現調→見積確定→着工→中間検査→竣工→引渡し（現状） |
| 新築案件 | 企画→基本設計→実施設計→確認申請→着工→上棟→竣工→引渡し |
| 開発/企画 | 企画→提案→承認→完了 |
| SNS | 企画→制作→投稿→分析 |
| セミナー/イベント | 企画→準備→集客→開催→フォロー |
| その他 | 未着手→進行中→完了（手動） |

**実装内容:**
1. プロジェクトに `projectType` フィールドを追加
2. 種別ごとのマイルストーンテンプレートを定義
3. プロジェクト作成/編集画面で種別に応じたマイルストーンフィールドを表示
4. ステータス自動計算を種別ごとのマイルストーンに対応

**備考:**
- 種別は後から追加できるようにする
- 将来的にはカスタムマイルストーンも検討

## 既知バグ・技術債

- [ ] Price IDハードコード整理
  - `functions/src/api/checkout.ts` にフォールバックPrice ID（`price_1SDiNTRpUEcUjSDNZa2AYMWq`）がハードコード
  - 本番ではFirebase Secretsの値が使われるが、コード上の整理が必要
  - 環境ごとのPrice ID管理方針の明確化

## ブラウザローカルLLM検討メモ

### タスク自動分類・リマインダーテキスト生成にローカルLLM検討

**ユースケース**:
1. タスク名・説明文からカテゴリ（設計、申請、施工管理等）を自動分類
2. マイルストーン遅延時のリマインダーテキストを自然な日本語で自動生成

**検討ポイント**:
- タスク分類は限定カテゴリへの振り分けなので、小型モデルでも精度が出る可能性
- リマインダーテキストは短文生成なので、ブラウザローカルLLMで十分対応可能
- Transformers.js のテキスト分類パイプラインが適している

**メリット**: APIコスト不要、オフライン動作、即座のレスポンス
**課題**: 建築用語の分類精度、モデルの初回ロード時間
**結論**: タスク分類は軽量モデルで実現可能。リマインダーテキスト生成はテンプレートベース + 変数埋め込みの方がシンプルで確実。ローカルLLMは「タスク自動分類」に限定して検討。
